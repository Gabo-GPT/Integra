# Render.com - Despliegue con un clic
# https://dashboard.render.com → New → Blueprint → Conectar repo

services:
  - type: web
    name: integra
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
